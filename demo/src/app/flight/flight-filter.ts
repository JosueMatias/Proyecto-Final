export class FlightFilter {
    from = ''; // Atributo que almacena el lugar de origen (inicializado a una cadena vacía)
    to = ''; // Atributo que almacena el lugar de destino (inicializado a una cadena vacía)
  }
  