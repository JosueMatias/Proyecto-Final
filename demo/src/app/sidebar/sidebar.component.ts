import { Component } from '@angular/core';

@Component({
  selector: 'sidebar-cmp', // Selector utilizado para incrustar este componente en la plantilla principal
  templateUrl: 'sidebar.component.html', // Plantilla HTML asociada al componente
})

export class SidebarComponent {
}
